---
phase: 05-enhance-ai-prompts
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/prompts/skeleton_stack_resolver_system.txt
  - backend/prompts/skeleton_tree_builder_system.txt
autonomous: true
requirements:
  - D-10
  - D-11
  - D-13
tags:
  - prompt-engineering
  - llm
  - skeleton

must_haves:
  truths:
    - "Stack resolver recognizes T3 stack, Django + React, and Rails as common stack inputs and produces sensible defaults"
    - "Tree builder Next.js template produces tailwind.config.ts, .env.example, and middleware.ts in the file list"
    - "Both prompts preserve their JSON-only output contract (no markdown fences, no prose)"
    - "Both prompts preserve the UNTRUSTED DATA guard"
  artifacts:
    - path: "backend/prompts/skeleton_stack_resolver_system.txt"
      provides: "Stack resolver with T3, Django+React, and Rails defaults"
      contains: "T3"
    - path: "backend/prompts/skeleton_tree_builder_system.txt"
      provides: "Tree builder with tailwind.config.ts, .env.example, middleware.ts in Next.js template"
      contains: "tailwind.config.ts"
  key_links:
    - from: "backend/agents/skeleton.py (NOT modified)"
      to: "backend/prompts/skeleton_stack_resolver_system.txt"
      via: "module-level read_text"
      pattern: "STACK_RESOLVER_PROMPT|skeleton_stack_resolver_system\\.txt"
    - from: "backend/agents/skeleton.py (NOT modified)"
      to: "backend/prompts/skeleton_tree_builder_system.txt"
      via: "module-level read_text"
      pattern: "TREE_BUILDER_PROMPT|skeleton_tree_builder_system\\.txt"
---

<objective>
Polish the two skeleton-generator prompts so they handle a broader set of common project stacks (T3, Django + React, Rails) and produce a more complete file scaffold for Next.js projects (adds `tailwind.config.ts`, `.env.example`, `middleware.ts`).

Purpose: D-10 and D-11 + D-13. These are minor refinements — both prompts keep their existing JSON-only output contract, UNTRUSTED DATA guard, role, and overall structure. Only the defaults/templates sections gain new entries.

Output: Updated `skeleton_stack_resolver_system.txt` (extended common-stack defaults) and `skeleton_tree_builder_system.txt` (extended Next.js template with new config files).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-enhance-ai-prompts/05-CONTEXT.md
@.planning/phases/05-enhance-ai-prompts/05-RESEARCH.md
@.planning/phases/05-enhance-ai-prompts/05-PATTERNS.md
@backend/prompts/skeleton_stack_resolver_system.txt
@backend/prompts/skeleton_tree_builder_system.txt

<interfaces>
<!-- Both prompts have stable JSON-output contracts that callers depend on. -->
<!-- This plan only ADDS to the templates/defaults sections — output schemas are unchanged. -->

`skeleton_stack_resolver_system.txt` output contract (UNCHANGED — caller is `backend/agents/skeleton.py`):
```json
{"frontend": "Next.js", "backend": "FastAPI", "db": "Neon"}
```
- frontend: string (default "Next.js")
- backend: string (default "FastAPI")
- db: string or null (default "Neon" if a database is mentioned, otherwise null)

`skeleton_tree_builder_system.txt` output contract (UNCHANGED):
```json
["frontend/app/layout.tsx", "frontend/app/page.tsx", ...]
```
- A JSON array of file path strings (no prose, no markdown fences)
- Under 25 entries
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend skeleton_stack_resolver_system.txt with T3, Django+React, and Rails common-stack defaults</name>
  <files>backend/prompts/skeleton_stack_resolver_system.txt</files>
  <read_first>
    - backend/prompts/skeleton_stack_resolver_system.txt (current — being extended; full structure preserved)
    - .planning/phases/05-enhance-ai-prompts/05-CONTEXT.md (D-10 wording)
    - .planning/phases/05-enhance-ai-prompts/05-PATTERNS.md (skeleton_stack_resolver_system.txt section: exact additions)
  </read_first>
  <behavior>
    - The UNTRUSTED DATA guard (lines 3) is preserved verbatim
    - The JSON output schema description (lines 5–8) is preserved verbatim
    - The "If no stack is stated, use defaults: ..." rule is preserved verbatim
    - The "Output ONLY the JSON object. No markdown fences, no explanation, no prose." line is preserved verbatim
    - The example output line is preserved verbatim
    - A NEW section is added (between the existing rules and the example) titled "Common stack defaults:" with three entries: T3 Stack, Django + React, Rails
    - The existing Next.js / FastAPI / Neon defaults are NOT removed
  </behavior>
  <action>
    Edit `backend/prompts/skeleton_stack_resolver_system.txt`. Insert a new "Common stack defaults:" block AFTER the "Rules:" section (after the line `- Output ONLY the JSON object. No markdown fences, no explanation, no prose.`) and BEFORE the "Example output:" line.

    The new block reads exactly:
    ```

    Common stack defaults — apply when the user mentions one of these by name:
    - "T3 stack" or "T3" → {"frontend": "Next.js", "backend": "Next.js API routes", "db": "Neon"}
    - "Django + React" or "Django and React" → {"frontend": "React", "backend": "Django", "db": "Postgres"}
    - "Rails" → {"frontend": "Rails ERB", "backend": "Rails", "db": "Postgres"}
    ```

    Do NOT change:
    - The role line ("You are a tech stack resolver...")
    - The UNTRUSTED DATA guard (line 3 — copy verbatim)
    - The JSON schema description block (lines 5–8 — keys frontend / backend / db with defaults)
    - The existing Rules block (must keep all current bullets including "Output ONLY the JSON object")
    - The example output line
  </action>
  <verify>
    <automated>grep -q "T3 stack" backend/prompts/skeleton_stack_resolver_system.txt &amp;&amp; grep -q "Django + React" backend/prompts/skeleton_stack_resolver_system.txt &amp;&amp; grep -q "\"Rails\"" backend/prompts/skeleton_stack_resolver_system.txt &amp;&amp; grep -q "UNTRUSTED DATA" backend/prompts/skeleton_stack_resolver_system.txt &amp;&amp; grep -q "Output ONLY the JSON object" backend/prompts/skeleton_stack_resolver_system.txt</automated>
  </verify>
  <acceptance_criteria>
    - File contains the substring "T3 stack" (or "T3 Stack" / "T3")
    - File contains the substring "Django + React"
    - File contains the substring "Rails" appearing in the new common-stack block
    - File contains the existing substring "UNTRUSTED DATA" (guard preserved)
    - File contains the existing substring "Output ONLY the JSON object" (output contract preserved)
    - File contains the existing substring `defaults: {"frontend": "Next.js", "backend": "FastAPI", "db": "Neon"}` (existing default rule preserved)
    - File contains the existing substring "Example output" (example preserved)
    - File length grew by at least 200 chars vs the current ~700-byte version (new block adds 3 stack entries) — `wc -c backend/prompts/skeleton_stack_resolver_system.txt` returns > 900
  </acceptance_criteria>
  <done>skeleton_stack_resolver_system.txt now contains common-stack defaults for T3, Django + React, and Rails, while preserving the UNTRUSTED DATA guard, the JSON-only output contract, the existing default rule, and the example output line.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend skeleton_tree_builder_system.txt Next.js template with tailwind.config.ts, .env.example, middleware.ts</name>
  <files>backend/prompts/skeleton_tree_builder_system.txt</files>
  <read_first>
    - backend/prompts/skeleton_tree_builder_system.txt (current — being extended; full structure preserved)
    - .planning/phases/05-enhance-ai-prompts/05-CONTEXT.md (D-11 wording)
    - .planning/phases/05-enhance-ai-prompts/05-PATTERNS.md (skeleton_tree_builder_system.txt section: exact additions)
  </read_first>
  <behavior>
    - The role line, UNTRUSTED DATA guard, and Rules block (including "Output ONLY a JSON array of strings. No prose, no markdown fences." and "Keep the list under 25 entries") are preserved verbatim
    - The Next.js frontend template gains three new entries: `frontend/middleware.ts`, `frontend/tailwind.config.ts`, `frontend/.env.example`
    - The existing Next.js entries (`frontend/app/layout.tsx`, `frontend/app/page.tsx`, `frontend/components/Header.tsx`, `frontend/lib/`, `frontend/public/`, `frontend/next.config.ts`, `frontend/package.json`, `frontend/tsconfig.json`) are preserved
    - The FastAPI backend template is NOT modified
    - The Neon / Postgres template is NOT modified
    - The example output JSON array is updated to include the three new Next.js entries (sorted alphabetically with the existing entries) so the example stays consistent with the template
  </behavior>
  <action>
    Edit `backend/prompts/skeleton_tree_builder_system.txt`. Make exactly two changes:

    **Change 1: Extend the Next.js frontend template** (current lines 13–21).
    Replace the current Next.js template block:
    ```
    Next.js frontend:
      - frontend/app/layout.tsx
      - frontend/app/page.tsx
      - frontend/components/Header.tsx
      - frontend/lib/
      - frontend/public/
      - frontend/next.config.ts
      - frontend/package.json
      - frontend/tsconfig.json
    ```
    with:
    ```
    Next.js frontend:
      - frontend/app/layout.tsx
      - frontend/app/page.tsx
      - frontend/middleware.ts
      - frontend/components/Header.tsx
      - frontend/lib/
      - frontend/public/
      - frontend/next.config.ts
      - frontend/tailwind.config.ts
      - frontend/.env.example
      - frontend/package.json
      - frontend/tsconfig.json
    ```

    **Change 2: Update the example output JSON array** (current line 39).
    Replace the existing example array with the new 19-entry array (alphabetically sorted, including the three new Next.js entries):
    ```
    Example output for {"frontend": "Next.js", "backend": "FastAPI", "db": "Neon"}:
    ["backend/.env.example","backend/agents/","backend/api/","backend/db/","backend/main.py","backend/models/","backend/requirements.txt","backend/utils/","frontend/.env.example","frontend/app/layout.tsx","frontend/app/page.tsx","frontend/components/Header.tsx","frontend/lib/","frontend/middleware.ts","frontend/next.config.ts","frontend/package.json","frontend/public/","frontend/tailwind.config.ts","frontend/tsconfig.json","migrations/001_init.sql"]
    ```

    Do NOT change:
    - The role line ("You are a project scaffolding expert...")
    - The UNTRUSTED DATA guard
    - The Rules block (output JSON array, no prose, under 25 entries, "/" separators)
    - The FastAPI backend template
    - The Neon / Postgres template
    - The "Combine the templates appropriate to the given stack..." instruction line

    Verify the example array still has < 25 entries (the new array has 20, which is well under the limit).
  </action>
  <verify>
    <automated>grep -q "frontend/tailwind.config.ts" backend/prompts/skeleton_tree_builder_system.txt &amp;&amp; grep -q "frontend/.env.example" backend/prompts/skeleton_tree_builder_system.txt &amp;&amp; grep -q "frontend/middleware.ts" backend/prompts/skeleton_tree_builder_system.txt &amp;&amp; grep -q "UNTRUSTED DATA" backend/prompts/skeleton_tree_builder_system.txt &amp;&amp; grep -q "Output ONLY a JSON array" backend/prompts/skeleton_tree_builder_system.txt &amp;&amp; python3 -c "
import re
content = open('backend/prompts/skeleton_tree_builder_system.txt').read()
# Find example output array — first '[' after 'Example output'
i = content.find('Example output')
arr_start = content.find('[', i)
arr_end = content.find(']', arr_start)
arr_str = content[arr_start:arr_end+1]
import json
arr = json.loads(arr_str)
assert 'frontend/tailwind.config.ts' in arr, 'tailwind missing from example'
assert 'frontend/.env.example' in arr, '.env.example missing from example'
assert 'frontend/middleware.ts' in arr, 'middleware missing from example'
assert len(arr) &lt; 25, f'example array has {len(arr)} entries, must be &lt; 25'
print('OK', len(arr), 'entries')
"</automated>
  </verify>
  <acceptance_criteria>
    - File contains the substring "frontend/tailwind.config.ts"
    - File contains the substring "frontend/.env.example"
    - File contains the substring "frontend/middleware.ts"
    - File still contains "UNTRUSTED DATA" (guard preserved)
    - File still contains "Output ONLY a JSON array of strings" (output contract preserved)
    - File still contains "Keep the list under 25 entries" (size limit preserved)
    - File still contains "FastAPI backend:" (FastAPI template preserved)
    - File still contains "migrations/001_init.sql" (Neon template preserved)
    - The example output JSON array (everything between the first `[` and matching `]` after "Example output") parses as valid JSON via `python3 -c "import json; json.loads(...)"` and contains all three new path strings
    - The example array has fewer than 25 entries
  </acceptance_criteria>
  <done>skeleton_tree_builder_system.txt now produces a Next.js scaffold including `tailwind.config.ts`, `.env.example`, and `middleware.ts`, with the example output JSON array updated to match. The UNTRUSTED DATA guard, JSON-only output contract, and 25-entry limit are preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend → backend `/api/skeleton` | User-supplied `project_md` crosses to backend; consumed by stack-resolver and tree-builder LLMs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Tampering / Information Disclosure (Prompt Injection — V5) | `skeleton_stack_resolver_system.txt` and `skeleton_tree_builder_system.txt` (both consume user-derived content) | mitigate | Both prompts already contain the UNTRUSTED DATA guard; tasks 1 and 2 PRESERVE it verbatim. Acceptance criteria explicitly verify `grep -q "UNTRUSTED DATA"` passes after the edits. |
| T-05-09 | Tampering (LLM emits non-JSON output, downstream parser crashes) | callers in `backend/agents/skeleton.py` | mitigate | Both prompts preserve "Output ONLY the JSON ..." instructions verbatim. The callers' existing JSON parsing + error handling are out of scope for this plan. |
</threat_model>

<verification>
**Wave 1, Plan 03 verification (after both tasks):**

1. **Static checks (run from repo root):**
   - `grep -q "T3" backend/prompts/skeleton_stack_resolver_system.txt`
   - `grep -q "Django" backend/prompts/skeleton_stack_resolver_system.txt`
   - `grep -q "frontend/middleware.ts" backend/prompts/skeleton_tree_builder_system.txt`
   - `grep -q "frontend/tailwind.config.ts" backend/prompts/skeleton_tree_builder_system.txt`
   - `grep -q "frontend/.env.example" backend/prompts/skeleton_tree_builder_system.txt`
   - Both files still contain "UNTRUSTED DATA"

2. **Example-output JSON validity check (run from repo root):**
   ```
   python3 -c "
   import json
   c = open('backend/prompts/skeleton_tree_builder_system.txt').read()
   i = c.find('Example output')
   s = c.find('[', i); e = c.find(']', s) + 1
   arr = json.loads(c[s:e])
   assert len(arr) < 25 and 'frontend/tailwind.config.ts' in arr
   print('OK', len(arr))
   "
   ```

3. **Skeleton agent import smoke test (from `backend/`, ensures the prompts still load at module startup without error):**
   ```
   python -c "from agents.skeleton import graph; print('OK')"
   ```

4. **Manual UI test (after deployment):**
   Generate a skeleton from a project.md mentioning "T3 stack" — verify the resolver returns `{"frontend": "Next.js", "backend": "Next.js API routes", "db": "Neon"}` and the file list includes `frontend/tailwind.config.ts`, `frontend/.env.example`, `frontend/middleware.ts`.
</verification>

<success_criteria>
- [ ] `skeleton_stack_resolver_system.txt` lists T3, Django + React, and Rails as common-stack defaults with appropriate frontend/backend/db values
- [ ] `skeleton_tree_builder_system.txt` Next.js template includes `frontend/tailwind.config.ts`, `frontend/.env.example`, and `frontend/middleware.ts`
- [ ] Both files preserve the UNTRUSTED DATA guard, the JSON-only output contract, and (for the tree builder) the under-25-entries cap
- [ ] The example output JSON array in tree builder parses as valid JSON and includes all three new entries
- [ ] `cd backend && python -c "from agents.skeleton import graph"` runs cleanly (prompts load at module import)
</success_criteria>

<output>
After completion, create `.planning/phases/05-enhance-ai-prompts/05-03-SUMMARY.md` documenting:
- The exact wording chosen for each new common-stack default in stack_resolver
- The final ordering of the Next.js template entries (alphabetical or grouped)
- Confirmation of the example array's entry count (must be < 25)
- Any deviations from the templates listed in PATTERNS.md
</output>
