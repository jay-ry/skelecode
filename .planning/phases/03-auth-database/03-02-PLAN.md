---
phase: 03-auth-database
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - frontend/migrations
autonomous: false
requirements:
  - DB-PUSH
user_setup:
  - service: neon
    why: "Serverless Postgres for projects + sprints persistence"
    env_vars:
      - name: DATABASE_URL
        source: "https://console.neon.tech -> Project -> Connection Details -> 'Connection string' (URL) — copy the full postgresql:// string"
    dashboard_config:
      - task: "Create a new Neon project (free tier) and copy the pooled connection string"
        location: "https://console.neon.tech -> Create Project"

must_haves:
  truths:
    - "Live Neon database has a `projects` table matching schema.ts"
    - "Live Neon database has a `sprints` table with FK referencing projects.id"
    - "Schema column names/types match lib/db/schema.ts (integration-level correctness, not just type-level)"
  artifacts: []
  key_links:
    - from: "Neon `projects` table"
      to: "Neon `sprints` table"
      via: "project_id uuid FK references projects(id) ON DELETE CASCADE"
      pattern: "Cascade FK visible in Neon table editor / pg_catalog"
---

<objective>
[BLOCKING] Push Drizzle schema to a live Neon Postgres. TypeScript compiles without this step, so it would create a false-positive verification state — every downstream API route would fail at runtime because the tables do not exist.

Purpose: Without a pushed schema, Plan 04 API routes would compile but crash on first query. This plan forces the user to provision Neon and run the push before any route handler is written.

Output: Live `projects` and `sprints` tables in Neon, matching `frontend/lib/db/schema.ts` column-for-column.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-auth-database/03-CONTEXT.md
@.planning/phases/03-auth-database/03-RESEARCH.md
@frontend/lib/db/schema.ts
@frontend/drizzle.config.ts
@frontend/.env.example
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] User provisions Neon + sets DATABASE_URL, then Claude runs drizzle-kit push</name>
  <files>frontend/.env.local, frontend/.env</files>
  <read_first>
    - frontend/.env.example (template created by Plan 01 Task 2 — source of all required env var names)
    - frontend/drizzle.config.ts (confirms `config({ path: ".env.local" })` is present)
    - .planning/phases/03-auth-database/03-RESEARCH.md (Pitfall 5 lines 431-436 — drizzle-kit reads .env not .env.local)
  </read_first>
  <action>
    This task requires a human-only setup step (creating a Neon account and copying a secret). Claude CANNOT bypass — but Claude WILL run the push command automatically once the user has populated `DATABASE_URL`.

    ### User steps (do these BEFORE typing "ready")

    1. Visit https://console.neon.tech and sign in (or create a free account).
    2. Click "Create Project" -> name it `skelecode` (region: closest to you) -> Create.
    3. Copy the "Connection string" (pooled). It looks like:
       `postgresql://neondb_owner:xxx@ep-xxxxx-pooler.us-east-1.aws.neon.tech/neondb?sslmode=require`
    4. Create `frontend/.env.local` from the template by running:
       ```
       cp frontend/.env.example frontend/.env.local
       ```
    5. Open `frontend/.env.local` and REPLACE the placeholder `DATABASE_URL=postgresql://user:pass@ep-xxx.neon.tech/neondb?sslmode=require` line with your real Neon connection string from step 3. Save.
    6. (Pitfall 5 workaround — belt-and-suspenders) Create `frontend/.env` with the same `DATABASE_URL` so that `drizzle-kit` CLI reads it even if the dotenv import in `drizzle.config.ts` fails at runtime:
       ```
       echo "DATABASE_URL=<your-neon-url>" > frontend/.env
       ```
       Note: `frontend/drizzle.config.ts` already calls `config({ path: ".env.local" })` so this is redundant — but harmless and protects against config bugs.
    7. Type `ready` in the chat (or whatever resume-signal the executor asks for).

    ### Claude steps (after user types "ready")

    Run each of these commands IN ORDER. STOP and surface the error to the user if any step fails.

    1. Verify `.env.local` has a real Neon URL (not the placeholder):
       ```
       cd frontend && grep -E '^DATABASE_URL=postgresql://[^ ]+neon\.tech' .env.local && \
         ! grep -E '^DATABASE_URL=postgresql://user:pass@ep-xxx' .env.local
       ```
       If grep returns nothing OR matches the placeholder, STOP and ask the user to redo steps 4-5.

    2. Run drizzle-kit push to create the schema in Neon:
       ```
       cd frontend && npx drizzle-kit push
       ```
       Expected stdout: a list of `projects` and `sprints` table creations, followed by `Changes applied` (or an interactive prompt). If a prompt appears, accept `Yes, I want to execute all statements`.

    3. Idempotency check — run push a SECOND time. It must report no changes:
       ```
       cd frontend && npx drizzle-kit push
       ```
       Expected stdout contains `No changes detected` (or equivalent). Any other output means the live schema drifts from `schema.ts` — STOP and investigate.

    4. Smoke-test that TypeScript still builds (no regression from schema push):
       ```
       cd frontend && npm run build
       ```
       Must exit 0.

    Do NOT run `drizzle-kit generate` or create migration files — the CONTEXT.md locks `push` workflow for MVP.
    Do NOT commit `.env.local` or `.env` — they contain secrets and are git-ignored by Next.js defaults.
  </action>
  <acceptance_criteria>
    - `frontend/.env.local` file exists and contains a line matching the regex `^DATABASE_URL=postgresql://[^ ]+neon\.tech` (real Neon URL, not the placeholder)
    - `frontend/.env.local` DOES NOT contain any line starting with `NEXT_PUBLIC_DATABASE_URL` (DATABASE_URL is server-only per Threat T-03-I-02)
    - First `cd frontend && npx drizzle-kit push` exits 0 and reports that tables were created (stdout contains `Changes applied` or equivalent success marker)
    - Second `cd frontend && npx drizzle-kit push` exits 0 and reports `No changes detected` (idempotency proves schema.ts matches live DB)
    - `cd frontend && npm run build` exits 0 after the push
    - `.gitignore` check (grep `\.env\.local` in any gitignore under frontend or repo root) confirms `.env.local` is git-ignored (Next.js default includes this — verify via `git check-ignore frontend/.env.local`)
  </acceptance_criteria>
  <resume-signal>Type "ready" after completing user steps 1-6 above, or describe any error encountered.</resume-signal>
  <verify>
    <automated>cd frontend && npx drizzle-kit push</automated>
  </verify>
  <done>Live Neon database has projects + sprints tables. drizzle-kit push is idempotent. .env.local contains real DATABASE_URL and is git-ignored. TypeScript still compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer machine -> Neon SaaS | `DATABASE_URL` secret flows over TLS (sslmode=require enforced) |
| `.env.local` -> git | `.env.local` is git-ignored; must never be committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-I-03 | Information disclosure | `.env.local` | mitigate | Acceptance criteria runs `git check-ignore` to confirm `.env.local` is git-ignored (Next.js `.gitignore` default). Grep also enforces DATABASE_URL has NO `NEXT_PUBLIC_` prefix. |
| T-03-T-02 | Tampering | `drizzle-kit push` execution | mitigate | Idempotency check (second push MUST report "No changes detected") verifies the applied schema matches `schema.ts` exactly, not some drifted intermediate state. |
| T-03-D-01 | Denial of service | Neon free tier | accept | Free tier limits are adequate for MVP validation; not a phase-level concern. |
</threat_model>

<verification>
- `grep -E "^DATABASE_URL=postgresql://" frontend/.env.local` returns exactly 1 line with a real Neon URL
- `grep "NEXT_PUBLIC_DATABASE_URL" frontend/.env.local` returns nothing
- `git check-ignore frontend/.env.local` exits 0 (file is ignored)
- `cd frontend && npx drizzle-kit push` (run twice) — second run reports no changes
- `cd frontend && npm run build` exits 0
</verification>

<success_criteria>
Neon database is live and schema matches. Plan 04 API routes will have tables to query. Phase cannot pass verification without this plan's task passing.
</success_criteria>

<output>
After completion, create `.planning/phases/03-auth-database/03-02-SUMMARY.md` documenting:
- That Neon push succeeded
- Version of drizzle-kit used (from `npx drizzle-kit --version`)
- REDACTED DATABASE_URL shape (write `postgresql://***@ep-***.neon.tech/neondb?sslmode=require` — NEVER paste the real URL)
- Confirmation that second push was idempotent
- Confirmation that `.env.local` is git-ignored
</output>
